name: smollm2
description: Pure Dart inference engine for SmolLM2 language models, delivering surprisingly capable local LLM results without requiring CUDA.
version: 1.0.1
repository: https://github.com/gmpassos/smollm2

environment:
  sdk: ^3.10.0

executables:
  smollm2:
  export_smollm2:

dependencies:
  collection: ^1.19.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dependency_validator: ^5.0.5
  huggingface_downloader: ^1.0.0
  path: ^1.9.0
